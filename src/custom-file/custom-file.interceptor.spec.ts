import { CustomFileInterceptor } from './custom-file.interceptor';

describe('CustomFileInterceptor', () => {
  it('should be defined', () => {
    expect(new CustomFileInterceptor()).toBeDefined();
  });
});
